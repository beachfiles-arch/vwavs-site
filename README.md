# VWAVS — TickSense

Public signal stream for the VWAVE TickSense detection system.

This repo is auto-generated by `~/Desktop/v24/rover_autopublish.sh` on a 5-minute
schedule. **Do not edit files here directly** — they get overwritten on the next
generation cycle.

To rebuild manually:
```
python3 ~/Desktop/v24/rover_publish.py
```

To force a deploy:
```
~/Desktop/v24/rover_autopublish.sh
```

## Pages

- `index.html` — landing + last 12h triggers
- `last24h.html` — full event stream + per-symbol summary
- `references.html` — curated track record (RLYB, PN, AKAN, HTCO)

## Hosting

Served via GitHub Pages from `main` branch root. Custom domain: `vwavs.com`.
